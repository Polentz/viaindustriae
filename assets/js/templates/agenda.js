window.addEventListener("load", () => {
    itemHeight();
    // handleHeaderOnScroll();
    searchFeature();
    sliderOpener();
});

window.addEventListener("resize", () => {
    itemHeight();
    // handleHeaderOnScroll();
});
